# T5 — Канон-верификация всех кабинетов: findings + план ре-пина

> **Дата:** 2026-06-12 · **Метод:** 4 канон-верификатора (MCP `project-docs` live, последовательно). Проверены факты 12 «устаревших» кабинетов против текущего канона (перестройка 10–12.06). · **Ветка:** `feature/canon-repin-go-roles`.

## 1. Подтверждённый актуальный пин-набор (bump TO)

| Док | Было (12 каб.) | Стало |
|---|---|---|
| `CONV-RBAC-DNM-001` | 1.4 | **1.6** |
| `CONV-ROLE-HIERARCHY-001` | 1.8 | **1.11** |
| `CONV-ROLES-DNM-001` | 1.3 | **1.6** |
| `SPEC-DNM-TZ-001` | 3.8 | **3.10** |
| `SPEC-M3-DNM-001` | 2.7 | **2.10** |
| `SPEC-FINANCE-REPORT-001` | 1.3 | **1.4** |
| `SPEC-M10-FRANCHISING-001` | 1.1 | **1.2** |
| `SPEC-DNM-RATING-001` | 1.2 | **1.5** |
| `SPEC-KPI-PAYOUT-001` | 1.1 | **1.3** |
| `REG-DNM-SOLARS-001` | 1.0 | **1.1** |
| `SPEC-DNM-REG-001` | 1.1 | **1.4** |
| `REG-DNM-SCHOOL-ADMIN-001` | 1.1 | **1.2** |
| `ARCH-ORG-001` | 1.2 | **1.4** |
| `ARCH-SERVICEMAP-001` | 1.1 | **1.5** |
| `REG-DNM-MARKETING-001` | 1.1 | **1.2** |
| **Без изменений** | — | `SPEC-DNM-FUNC-001` 2.3 · `REG-DNM-LESSON/CURATOR(-PARENTS)-001` 1.0 · `CONV-USER-ROLES-001` 1.3 · `SPEC-M11-FINANCE-001` 1.2 · `SPEC-CRM-TZ-001` 1.1 · `SPEC-DNM-REPORT-TEMPLATES-001` 2.0 · `ADR-0002` 1.1 |

> ⚠️ Не подтверждены в `list_index(apps=dnm)`: `REG-PARTNER-NET-001`, `REG-FRANCHISE-ORGPOLICY-001`, `ROLE-FRANCHISE-CURATOR-001` (источники `lead.ts`) — проверить вручную перед ре-пином (могли быть свёрнуты в decision-log).

## 2. Вердикты по кабинетам — три корзины

### 🟢 A. Clean-bump (факты держатся — только версии)
- **curator** — 3-фаза/ДЗ без баллов/финансы скрыты ✓. (Trial-runner — см. C1.)
- **parent** — ДЗ без баллов/непосещение→админ/рассрочка/ПДн ✓.

### 🟡 B. Bump + безопасный реврайт (дрейф РАТИФИЦИРОВАН каноном — снять устаревшие флаги)
- **child** — снять «⚠ на согласовании» с соларов/рейтинга/когорт: ратифицировано `SOLARS v1.1` + `RATING v1.5` + `M3 v2.10` (FR-M3-142/145/150).
- **methodist** — «divergent/canon-proposal» → **ратифицировано**: `M3 v2.10` FR-M3-013/101 перевёл конструктор программы на методиста (br7 утверждает, сам не правит). Оставить «КФ-ревью» как project-doc-нюанс (не в M3).
- **franchise-curator** — снять ⚠ с «методист в подчинении КФ» (теперь канон, `ROLES v1.6 §2б`); можно добавить «школьные ст.кураторы отчитываются КФ» (`OQ-ORG-04`).
- **finance** — **(а)** «Финансист ГО (общий ЛК Само Глобал)» → **фактическая ошибка**: `op-finansist-go-dnm` теперь ратифицированная ГО-роль ДНМ (`ROLES v1.6 §2а`, `HIERARCHY v1.11`), не внешний продукт. Переписать crossLink. **(б)** ярлык «Бухгалтер ДНМ» → «Бухгалтер школы» (rename 10.06; id сохранён). **(в)** назвать `op-finansist-go-dnm` в note «Вариант A».
- **lead** — Финансист ГО/Маркетолог ГО/Менеджер франшиз — теперь легит канон-роли (снять «заглушка/Само Глобал» framing у `finance-go`). **Авторинг программы — см. C3.**
- **guest** — un-stale «публичный каталог не специфицирован» (теперь `REG v1.4 §2`). **Trial-runner — см. C1.**
- **marketer** — снять «вне RBAC-матрицы v1.4»: теперь `RBAC v1.6 §9 «Маркетинговые ресурсы»` + роль Мрк (11-я). **Close-rate — см. C2.**

### 🔴 C. Открытый дрейф — НЕ править молча → canon-proposals + STATUS «Расхождения»
- **C1. Trial-runner (куратор vs менеджер)** — живое **само-противоречие канона**: `TZ v3.10 §13.4/§18.1` (пробное ведёт КУРАТОР, менеджер не ведёт) vs `REG v1.4 §3` (ведёт менеджер, гибко куратор); «гибкость пробного» — Волна 2, **не ратифицировано** (`M3 v2.10`). Атлас (sales/guest/curator) подаёт «менеджер ведёт сам» как решённое → **понизить до «на согласовании»**. Затрагивает: sales, guest, curator.
- **C2. marketer close-rate vs «ноль финансов»** (самый тяжёлый) — ядро marketer «сквозная атрибуция / close rate возвращается маркетологу» **противоречит** `RBAC v1.6 §9` + решение «ноль финансов» 11.06: Мрк **НЕ видит выручку/close rate** (у ГО); KPI = доля квалиф. лидов. → либо вынести close-rate-видимость на ГО, либо canon-proposal (owner-divergent). Решение владельца нужно.
- **C3. lead/methodist авторинг программы** — `OQ-ORG-03` **ОТКРЫТ**: 3 модели (RBAC §2 CRUD у br7 ↔ атлас «у КФ» ↔ решение «методист»). Перенацелить предложенного автора КФ→**методист**, proposal оставить.
- **C4. franchise — расцепление Франчайзи-партнёр/Директор** — `pr2` = собственник (владелец P&L), Директор школы = отдельная роль (`op-direktor-shkoly-dnm`, ратиф. 10.06). Заголовок `franchise.ts` «Франчайзи / директор» конфлейтит. «Вход как директор» (FR-M3-094) — ДЕРЖИТСЯ. → расцепить идентичность (но это меняет голден/тест franchise; взаимодействует с PR #10).
- **C5. senior-curator линия отчётности** — канон: in-school эскалация качества → **администратор** (`REG-SCHOOL-ADMIN v1.2`), ГО-данные → **Куратор франшиз** (`OQ-ORG-04`). Ни одна не указывает на «директора». T3 направил вертикаль на director (орг-подчинение по дереву Screenshot_4). Нюанс лейблов, не реверс. → уточнить.
- **C6. sales рассрочка** — `RBAC v1.6 §6` + `REG v1.4 §6`: рассрочку/график **ведёт менеджер (CRU)**, атлас недокредитовал (отдал финансам/админу). → поправить атрибуцию (безопасно? — фактический фикс, можно в B).

## 3. ГО-роли — маркеры в /map (решение владельца)
- Убрать **«Старший куратор ГО»** (`system-map.ts:29`) — `OQ-ORG-04` понизил, НЕ роль.
- **Маркетолог ГО / Финансист ГО / Менеджер франшиз** — теперь легит канон-роли v1.6: поправить подписи (не «заглушка»), оставить ГО-узлами иерархии + строки матрицы offCanon/scope. Добавить **РОП ГО** (структурный узел).
- НЕ строить полные кабинеты (Само Глобал/M10 вне ядра).

## 4. Таргет/СММ (решение D1)
Уже функции в `marketer.ts` (D3 Таргет + D4 СММ). Добавить заметку `OQ-ORG-01` (канон склоняется: `op-marketolog-dnm`=ГО, школа=СММ+Таргетолог сплит; владелец оставляет функциями — открыто).

## 5. План исполнения
- **T5a (применить сейчас):** ре-пин всех 12 кабинетов под §1 + корзина B (безопасные ратифицированные реврайты) + C6 (рассрочка=менеджер) + ГО-маркеры §3 + Таргет/СММ note §4. Тесты/голдены/STATUS.
- **T5b (зафиксировать, не править):** корзина C1–C5 → `docs/canon-proposals/2026-06-12-*` + STATUS «Расхождения». Owner-решения: C2 (marketer close-rate), C4 (franchise расцепление), C1 (trial-runner).
