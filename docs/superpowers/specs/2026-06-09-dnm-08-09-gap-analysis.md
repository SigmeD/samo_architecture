# Gap-анализ: встречи 08–09.06 ↔ атлас ДНМ (мастер-документ доработок)

**Дата:** 2026-06-09 · **Ветка:** `update_09_06`
**Метод:** 6 BA-агентов (read-only) против локального клона `D:\Projects\samo-docs` (встречи + follow-up + reconcile 16 расхождений) ↔ `content/cabinets/*.ts`.
**Источники намерения:** встречи `2026-06-08-dnm-soglasovanie-arhitektury.md`, `2026-06-09-dnm-finalizatsiya-start-samo-global.md` + их follow-up + `reports/2026-06-08-reconcile-dnm-folloap-tz-m3.md`.
**Назначение:** единый план доработок атласа. Правок много — структурированы по: (A) решения владельца/реверсы [НЕ править молча], (B) новые сущности, (C) аддитивные правки по кабинетам, (D) scope-границы.

> ⚠️ **Главный вывод:** атлас покрывает ~70% намерения встреч, но встреча **09.06 реверсит 2 центральных инварианта** (пробное «только куратор»; закрытие сделки «у админа»), а 08.06 — ещё 3 (финансовая свёртка, маркетинговый фонд, авторинг программы). Плюс **новая роль методиста** и **новые роли ГО**. Реверсы НЕ финальны (часть — «уточнить с франшизой завтра»). Правила атласа: расхождение намерение↔код **не править молча** — выносим как решение владельца + canon-proposal.

---

## A. Решения владельца / реверсы (БЛОКИРУЮТ молчаливую правку)

Каждый пункт меняет уже зафиксированное в атласе/canon-proposal. Перед правкой — подтверждение ПА.

| # | Тема | Атлас сейчас | Встреча (источник) | Действие | Файлы |
|---|---|---|---|---|---|
| **R1** | **Финансовая свёртка** | `finance.ts` намеренно слил «Финансист→Бухгалтер» в одну роль `op-finansist-dnm` | 08.06 §1.3: **две роли** — бухгалтер франшизы → **финансист ГО** → руководитель + «связка-акт» (reconcile 🔴 BUG-08-01) | Развести? Завести роль/кабинет финансиста ГО? | `finance.ts`, `system-map.ts` (2 блока на 1 slug) |
| **R2** | **Авторинг учебной программы** | Атлас отдал авторинг **куратору франшиз** (`lead.ts` поправка, `system-map` tag «право методист») | 08.06 §1/§3: автор = **методист** (новая роль); руководитель только утв./откл. (🔴 BUG-08-02) | Переназначить автора КФ→методист | `lead.ts`, `franchise-curator.ts`, `system-map.ts`, new `methodist.ts` |
| **R3** | **Геймификация куратора** | `curator.ts`: «ручное начисление соларов» | 08.06 §5.2 / 09.06 §1: куратор **дарит медальки/значки**, каждый несёт солар (3/5) → улетает ребёнку (🔴 BUG-08-04) | Медальки замещают ручной ввод или UI-обёртка? | `curator.ts`, `child.ts` |
| **R4** | **ДЗ блокирует переход** | `child.ts` инвариант (×4): «урочный тест и ДЗ **НЕ блокируют**, блокирует только итоговый тест» | 09.06 §2 / fu §3.4: «невыполнение ДЗ **блокирует переход**» | Развести: блокирует переход урок-в-урок (а) или только семестр/сертификат (б)? Атлас выбрал (б), встреча буквально (а). **Уточнить** | `child.ts` coreProcess.note + домен ДЗ |
| **R5** | **Пробный урок** | Жёстко «**ТОЛЬКО куратор**» (sales/guest + canon-proposal S1) | 09.06 §5/§6.2: **выбор «куратор / менеджер»** (универсал берёт сам). RBAC v1.4 был легитимен. ⚠ «уточнить с франшизой» | Сделать **конфигурируемой опцией** (default куратор), не переписывать в «менеджер» | `sales.ts`, `guest.ts`, canon-proposal `2026-06-08-sales/guest` |
| **R6** | **Закрытие сделки** | Жёстко «договор/оплата у **админа/финансов**, менеджер read» (canon-proposal S2) | 09.06 §6.3: договор+счёт **на менеджере** (ведёт от начала до конца). ⚠ «камень преткновения, финально с франшизой» | Ввести как **опцию закрытия на менеджере**, пометить «на согласовании» | `sales.ts`, canon-proposal `2026-06-08-sales` |
| **R7** | **Маркетинговый фонд** | Атлас **добавил** (lead.ts домен «Договоры·Фонд» + модуль `marketing-fund`, isNew; system-map handoff) | 08.06 §2: **убрать/скрыть** (спорный, отложен) (🟡 BUG-08-16) | Убрать из system-map + lead.ts | `system-map.ts`, `lead.ts` |
| **R8** | **Связь старший куратор → руководитель** | `senior-curator.ts` имеет прямой `crossLink toCabinet: "lead"` | 08.06 §7: **убрать** — СК школы только со ст.куратором ГО + школа (🟠 BUG-08-08) | Убрать crossLink→lead; адресат — ст.куратор ГО (новый) | `senior-curator.ts` |
| **R9** | **Три вида оплаты** | `parent.ts`: полная + рассрочка (единый механизм) | 08.06 §5.4: **(1)полная (2)посеместровая (3)помесячная** = внутр. рассрочка + **внешняя (банк-партнёр, комиссия банку)** (🔴 BUG-08-03) | Переопределить модель оплаты; новая сущность «банк-партнёр» | `parent.ts`, new canon-proposal |

> R5/R6 особо: на 08.06 позиция совпадала с атласом, **реверс произошёл 09.06** (новее → приоритетнее). 08.06 и 09.06 расходятся — фиксировать 09.06, но обе «не финальны» до встречи с франшизой.

---

## B. Новые сущности (создать)

| # | Сущность | Что это | Файлы |
|---|---|---|---|
| **N1** | **Кабинет методиста** (`methodist.ts`) | Новая роль ГО. Ядро «контур авторинга методики» (план → AI-сегментация → визуал видов → конструктор → утверждение руководителем → публикация кураторам). Домены: программа/методика, **конструктор видов уроков** ⭐, библиотека визуалов, цикл утверждения, методконтент Академии. Связи: → lead (gate), → curator (программа), ↔ franchise-curator (передача права авторинга) | new `content/cabinets/methodist.ts` + `index.ts` + `overview.ts` + `system-map.ts` + canon-proposal `2026-06-09-methodist-cabinet.md` |
| **N2** | **Роли ГО на карте** | Добавить в `overview`/`system-map`: финансист ГО (R1), методист (N1), **старший куратор ГО** (🟠 BUG-08-08), эксперты по направлениям (привлекаемые), **менеджер по продажам франшиз** (≠ менеджер школы; продажа франшиз живёт в lead) (🟡 BUG-08-14) | `overview.ts`, `system-map.ts` |
| **N3** | **Бизнес-академия как сквозной кабинет** | Решение: сквозной, **НЕ кабинет тренера** (08.06 §7). **Рекомендация — Вариант A:** усилить `overview.crossModules` (обучение по ролям, график, темы, тренеры/эксперты, онлайн/оффлайн, **хранение записей**) — прецедент «отдела качества» (функция, не кабинет). Вариант B (страница `/academy` как `/map`) — следующая фаза | `overview.ts` (crossModules), домены «Академия» в кабинетах |
| **N4** | **Исполнительный директор** (фигура-фильтр) | При нескольких школах аналитика идёт через него, франчайзи получает выборку (08.06 §1.3) | `system-map.ts` (hierarchyNote) |

---

## C. Аддитивные правки по кабинетам (не реверсы)

### C1. Ученик (`child.ts`) — свежий редизайн покрыл ~80%, осталось:
- ❌ **Переименовать домен «🎭 Когортный слой JUNIOR/SENIOR»** → понятное («Какие курсы проходит», младшая/старшая) — сейчас в коде ровно запрещённый заказчиком термин (09.06 §2, задача #1).
- ❌ **Виды уроков** — у урока есть ВИД со своим визуалом (солнышки/шипы/фигурки по типу и сложности: с ДЗ/без/СЦ/пробный/групповой/индивидуальный); конструктор — у методиста, но факт вида — в кабинете ученика (09.06 §3).
- ❌ **«Эффект гонки»** — балл/значок на ЛЮБОЕ действие, мотивация позитивом (явный принцип геймификации/дашборда) (09.06 §1).
- ❌ **Усреднение позиции рейтинга** — «вы 5-й», не раскрывать что таких 25 (09.06 §1).
- 🟡 **Переход 13→14** — есть «ритуал»; добавить: поздравительный **сертификат** «стал взрослым» + **разблокировка взрослого курса** + **родитель видит переход** (09.06 §2).
- ❌ **Блог** как раздел; ❌ **мультиязычность ИИ-аватара наставника** (ru/en/de — НЕ путать с языком обучения ru/kz/tj/uz); 🟡 **баллы в СУ**; 🟡 уточнить **«СЦ»** (вероятно «система ценностей», не СУ); ❌ видимость **совместного урока** родитель+ребёнок.

### C2. Родитель (`parent.ts`) — зрелый, осталось:
- ❌ **R9 три вида оплаты** (см. A) — главная дыра.
- ❌ **Универсальный кабинет / Global Samo** — «путь клиента шире пути ученика», переход во взрослую бизнес-академию (концепция, пометить «на подтверждении»; Само Глобал — соседний продукт).
- 🟡 **Трёхуровневый рейтинг ученика** (в группе/в школе/в проекте) — есть единая цифра, нет разбивки.
- 🟡 **Расписание по семестру** (12 недель ≈ 24 урока + фильтр по семестрам/курсам) — явно зафиксировать.
- 🟡 **Опросник → отчётность франшизы → куратор франшиз** (нет crossLink на franchise-curator).
- ✅ **Рейтинги школ родителю НЕ показывать** — соблюдено; добавить **явный инвариант-запрет** (профилактика).
- ❌/🟡 отложенные: новостная лента (маркетинг, «на подтверждении»); совместные задания младшая (с методистом); мультиопекунство ✅.

### C3. Куратор (`curator.ts`):
- 🟡 **Логика урока 11 шагов** (явный порядок) — сейчас «3 фазы», «основная часть» прячет 7 шагов (🟠 BUG-08-05).
- ❌ **«Отметить участников» = критичный шаг №2** + тиринг: не отмечен → пуш родителю → повтор → **звонок администратора** (не куратора) (🟡 BUG-08-13).
- 🟡 **Видеофиксация посещаемости** — 3–4 снимка с камеры/урок, сверка с отметкой (защита от «левачения») (🟠 BUG-08-07).
- 🟡 **Обучение куратора → через сквозную Бизнес-академию** (N3), а не локальный домен.
- 🔄 R3 медальки (OQ), 🔄 видеоуроки Zoom/YouTube→своё хранилище (BUG-08-06, развести проведение vs хранилище).
- ✅ безоценочная система, 3 цикла проверки — совпадают (уточнить триаду: атлас «ДЗ/эссе/бизнес-проект» vs встреча «ДЗ/классные/бизнес-проекты», BUG-08-12).

### C4. Старший куратор (`senior-curator.ts`):
- 🔴 **R8 убрать crossLink→lead**; адресат — ст.куратор ГО (N2).
- 🟡 **Упростить подачу цикла коучинга** («слишком сложно» — атлас детализирует 4 шага).
- ✅ двойное ядро, замена больного, аттестация — реализовано (опережает).

### C5. Менеджер (`sales.ts`):
- 🔄 **R5 пробное (опция куратор/менеджер)**, 🔄 **R6 закрытие сделки (опция на менеджере)**.
- 🟡 **KPI**: период созревания 3/7/12/30 дн; «степень закрытия» (1 из N); **время 1-го контакта до 40 мин** (норматив); каденс 6–8 ✅; источник ✅.
- ❌ **Рекомендация совместного урока** родитель+ребёнок (→ parent/child).
- 🟡 **Связи**: + франчайзи-партнёр, + РОП ГО; ⚠ **куратор франшиз СКРЫТ** от франчайзи (агрегация наверх) — добавить пометку (09.06 §6.6).
- ✅ карточка клиента (дубли/скоринг/протухшие), гость-онбординг.

### C6. Маркетолог (`marketer.ts`):
- 🟡 **Интеграции через AI/API** (Instagram-аккаунты + Google Ads/метрики) — явная механика.
- ❌ **Вкладка «Рекомендуемые материалы для продаж»** — топ-5 роликов по просмотрам со всей сети, автосбор статистики → менеджер берёт для клиента (+ блок у sales; crossLink marketer↔sales).
- 🟡 структура (генерация рекламы, СММ-центр точек, отчётность Академии) — уточнить формулировки.
- ❌ scope-note: переиспользование модуля для маркетолога Само Глобал (комментарий, не реализация).

### C7. Гость (`guest.ts`):
- 🔄 **R5 пробное** (синхронно с sales).
- ✅ гость-онбординг (демо до договора) — уже покрыт.

### C8. Системная карта / overview (`system-map.ts`, `overview.ts`):
- N2 роли ГО, N4 исполнительный директор, R1 финиерархия (звено финансист ГО в финпотоке), R2 авторинг→методист (ячейка матрицы), R7 убрать маркетинговый фонд из handoffs, N3 Бизнес-академия сквозная, **кросс-продажи ДНМ↔Само Глобал** граничная строка (D).

---

## D. Scope-границы (НЕ в атлас ДНМ)

- **Само Глобал — отдельный продукт** (старт со след. дня). Кабинет клиента (**Клиент ≠ Ученик**: клиент = любой на платформе, ученик = купил первый курс), удержание времени (соцсеть-привычка), контент основателя внутрь, счётчик 1–2 ч/день, AI-аватар основателя — **моделировать в отдельном проекте/атласе**, не в `content/cabinets/` ДНМ.
- **На стыке (можно отразить граничной заметкой в ДНМ-атласе):** кросс-продажи ДНМ↔Само Глобал; универсальность кабинета родителя (C2). Без углубления.

---

## E. Обезличивание (инвариант атласа — критично)

Встречи/follow-up **насыщены именами** — в атлас НЕ переносить (memory `atlas-impersonal-no-names`, `no-davlatov-royalty-share`):
- Павел Анатольевич / «≈ Довлатов» → **руководитель проекта** / **основатель**; «AI-аватар Довлатова» → **AI-аватар основателя/наставника**.
- Айгерим → куратор франшиз · Анастасия → финансист (+ функцию «помощник» не вносить) · Сережан/Динара → франчайзи-партнёр/практик · Джамшед → не вносить · Андрей → маркетолог · «Леша» → разработчик (не вносить).
- Банк (внешняя рассрочка) → роль «банк-партнёр», без конкретного банка.
- Доля роялти (20%/50%) — **не отображать**. Guard `tests/content/impersonal.test.ts` покроет новые файлы — прогнать `npm run test` после правок.

---

## F. План правок (фазы)

**Фаза 0 — решения владельца (СНАЧАЛА).** Вынести R1–R9 (+ N1 актор авторинга, R3 медальки, R4 ДЗ-блок) как вопросы ПА. Без них правки A-блока — гадание. → собрать в один опросник владельцу.

**Фаза 1 — аддитивные правки (не требуют решений), TDD по кабинету:**
- C1 ученик (переименовать когортный слой, виды уроков, эффект гонки, усреднение, переход 13→14, блог, мультиязык аватара, СУ-баллы).
- C2 родитель (3-уровневый рейтинг, расписание-семестр, опросы→КФ, инвариант-запрет рейтингов школ, отложенные).
- C3 куратор (11 шагов, отметить участников + тиринг, видеофиксация, Академия-сквозная).
- C5/C6 менеджер/маркетолог (KPI, совместный урок, скрытие КФ; интеграции API, топ-5 роликов).
- C4 ст.куратор (упростить коучинг).

**Фаза 2 — реверсы (после решений Фазы 0):** R1–R9 + переформулировать canon-proposal'ы (sales S1/S2, guest, новый parent-оплата, новый methodist).

**Фаза 3 — новые сущности:** N1 кабинет методиста, N2 роли ГО, N3 Бизнес-академия сквозная, N4 исполнительный директор, R8 связь ст.куратора.

**Фаза 4 — карта/overview (C8) + кросс-продажи граница (D).**

Каждая правка кабинета: TDD (инвариант-тест → контент → golden → vision-gate 2) + обезличивание + ⚠/canon-proposal на расхождениях. Verify: typecheck · test · build · e2e · vision gate.

---

## G. Reconcile-баглист (16 расхождений, ссылка)

Детали severity и текст — `D:\Projects\samo-docs\docs\reports\2026-06-08-reconcile-dnm-folloap-tz-m3.md`. 🔴 владельцу: BUG-08-01 (финиерархия R1), -02 (методист R2), -03 (оплата R9), -04 (солары↔медальки R3). 🟠 proposal-diff (TZ v3.9 / M3 v2.8): -05 (11 шагов), -06 (видеоуроки), -07 (видеофиксация), -08 (ст.куратор ГО R8), -09 (Академия сквозная N3), -14 (менеджер франшиз). 🟡 low: -12 (триада циклов), -13 (тиринг непосещения), -16 (маркетинговый фонд R7).
